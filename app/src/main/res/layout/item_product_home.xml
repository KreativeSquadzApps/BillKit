<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="productHome"
            type="com.kreativesquadz.billkit.model.Product" /> <!-- Replace "your.package.name" with your actual package name -->

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="5dp"
        app:cardBackgroundColor="@color/lite_grey_200"
        app:cardElevation="2dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="10dp">

            <!-- Customer Name -->
            <TextView android:fontFamily="@font/montserrat_regular"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{productHome.productName}"
                android:textSize="16sp"
                android:id="@+id/tvProductName"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:textStyle="bold"
                android:textColor="@color/text_color_heading"
                android:paddingBottom="4dp"
                tools:text="Name" />

            <TextView android:fontFamily="@font/montserrat_regular"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toBottomOf="@+id/tvProductName"
                app:layout_constraintStart_toStartOf="parent"
                android:text="@{productHome.productPrice}"
                android:textColor="@color/text_primary_n_dark"
                android:paddingBottom="4dp"
                android:textStyle="bold"
                tools:text="Rs 200" />

            <TextView android:fontFamily="@font/montserrat_regular"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toBottomOf="@+id/tvProductName"
                app:layout_constraintEnd_toEndOf="parent"
                app:textColorRnG="@{productHome.productStock}"
                android:text="@{productHome.productStock}"
                android:textStyle="bold"
                android:paddingBottom="4dp"
                tools:text="200" />



        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>
