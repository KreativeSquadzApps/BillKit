<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="savedOrder"
            type="com.kreativesquadz.billkit.model.SavedOrder" /> <!-- Replace "your.package.name" with your actual package name -->

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="5dp"
        app:cardBackgroundColor="@color/lite_grey_200"
        app:cardElevation="2dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="7"
            android:padding="10dp">

            <!-- Customer Name -->
            <TextView android:fontFamily="@font/montserrat_regular"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{savedOrder.orderName}"
                android:layout_weight="2"
                android:textSize="16sp"
                android:id="@+id/tvProductName"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:textStyle="bold"
                android:textColor="@color/text_color_heading"
                android:paddingBottom="4dp"
                tools:text="Name" />

            <TextView android:fontFamily="@font/montserrat_regular"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{savedOrder.totalAmount}"
                android:layout_weight="2"
                android:textSize="16sp"
                android:id="@+id/tvTotalPrice"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:textStyle="bold"
                android:textColor="@color/text_color_heading"
                android:paddingBottom="4dp"
                tools:text="Name" />

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/trash"
                android:id="@+id/deleteOrder"
                android:layout_weight="3"/>




        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
